const favoriteSeriesCreateSchema = require('./favoriteSeriesCreateSchema');
const favoriteSeriesParamsSchema = require('./favoriteSeriesParamsSchema');
const idParamSchema = require("../shared/idParamSchema");

module.exports = {
    favoriteSeriesCreateSchema,
    favoriteSeriesParamsSchema,
    idParamSchema,
};
